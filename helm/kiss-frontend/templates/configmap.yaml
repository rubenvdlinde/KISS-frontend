apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kiss-frontend.fullname" . }}
  labels:
  {{- include "kiss-frontend.labels" . | nindent 4 }}
data:
  env.js: |
    // This file is automatically generated
      window.pubBeheerUrl = '{{ .Values.config.pubBeheerUrl }}';
      window.organisatieIds = ["{{ join "\",\"" .Values.config.organizationIds }}"];
      window.gatewayBaseUri = '{{ .Values.config.gatewayBaseUri }}';
